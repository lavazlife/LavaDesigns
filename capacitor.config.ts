import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.liquidlavaz.nexus',
  appName: 'liquidlavaz',
  webDir: 'dist/app/browser'
};

export default config;
